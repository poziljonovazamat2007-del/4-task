#include <stdio.h>

int main()
{
    int non, sum, narx;

    printf("Non narxini kiriting: ");
    scanf("%d", &narx);
    printf("Nechta non olmoqchsiz: ");
    scanf("%d", &non);
    sum= non * narx;
    printf("%d so'm", sum );
    
    return 0;
}
